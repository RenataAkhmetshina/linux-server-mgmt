# Installation Guide

1. Clone the repository:
   `git clone https://github.com/USERNAME/linux-server-mgmt.git`
2. Grant execution permissions:
   `chmod +x scripts/*.sh`
3. Prerequisites: Bash 4.0 or higher.
